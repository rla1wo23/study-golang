package main

import(
	"fmt"
	"os"
	"bufio"
)
var k int
var reader *bufio.Reader=bufio.NewReader(os.Stdin)
var writer *bufio.Writer=bufio.NewWriter(os.Stdout)
func brute(cnt int){
	
}
func main(){
	
}